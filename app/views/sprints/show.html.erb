<div class="contextual">
  <%= link_to(l(:button_edit), edit_sprint_path(@sprint), :class => 'icon icon-edit') if User.current.allowed_to?(:edit_sprints, @sprint.project) %>
</div>

<h2><%= h(@sprint.name) %></h2>
